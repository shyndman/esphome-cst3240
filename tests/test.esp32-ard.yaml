esphome:
  name: cst3240-test-esp32-ard

esp32:
  board: esp32dev
  framework:
    type: arduino

wifi:
  ssid: "test"
  password: "testpass"

logger:
api:
ota:
  - platform: esphome

external_components:
  - source: ../components
    components: [cst3240]

substitutions:
  clk_pin: GPIO0
  mosi_pin: GPIO2
  cs_pin: GPIO4
  dc_pin: GPIO5
  disp_reset_pin: GPIO12
  scl_pin: GPIO13
  sda_pin: GPIO14
  interrupt_pin: GPIO15
  reset_pin: GPIO16

<<: !include common.yaml
